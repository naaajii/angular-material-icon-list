import { FontSetURLPipe } from './font-set-url.pipe';

describe('FontSetURLPipe', () => {
  it('create an instance', () => {
    const pipe = new FontSetURLPipe();
    expect(pipe).toBeTruthy();
  });
});
